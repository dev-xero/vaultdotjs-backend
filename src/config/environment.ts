/**
 * Constant mapping of the possible application environments:
 * PROD -> The server is running in a production environment.
 * DEV -> The server is running in a development environment.
 */
export const environment = {
    PROD: 'prod',
    DEV: 'dev',
};
